var arr=[3,4,5,63,27,9];

//for finding the max value we need to apply Math.max.apply(null,arr)


var x=Math.max.apply(null,arr);
console.log(x);

//for minimum number use the Math.min.apply(null,arr)
var y=Math.min.apply(null,arr);
console.log(y);
