var x=14.56;
//toExponential() methods returns a string which is in exponent form
//and we specify a number inside the toExponent method that is nothing but how many number exists after the
//decimal point...and it also rounded the number
console.log(x.toExponential(0));
console.log(x.toExponential(2));
console.log(x.toExponential(4));



var y=9.23;
console.log(y.toExponential(0));
console.log(y.toExponential(2));
console.log(y.toExponential(4));
